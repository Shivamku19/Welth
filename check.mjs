import { db } from './lib/prisma.js'; async function test() { const users = await db.user.findMany({ include: { transactions: true } }); for (const user of users) { console.log('User: ' + user.name + ' (' + user.email + ') - ID: ' + user.id); console.log('Total Transactions: ' + user.transactions.length); if (user.transactions.length > 0) { console.log('First Date: ' + user.transactions[0].date); console.log('Last Date: ' + user.transactions[user.transactions.length-1].date); } } } test().then(() => process.exit(0));
